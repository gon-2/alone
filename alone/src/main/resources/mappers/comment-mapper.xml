<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.alone.mappers.comment">
	
	<!-- 댓글 입력 -->
	<insert id="insertcomment">
		insert into tbl_class_board_comment(class_board_comment_content , class_board_comment_userid , class_board_comment_date)
		values(#{class_board_comment_content} , #{class_board_comment_userid} , sysdate)
   	</insert>
   	<!-- 댓글 조회 -->
   	<select id="selectcomment" resultType="RegistCommentVo">
   		select * from tbl_class_board_comment
   	</select>	
	
</mapper>